stages:
  - deploy
    
provision_raspberrypi:
  stage: deploy
  when: manual
  tags:
    - ansible
  script:
    - ansible-playbook -i hosts provision_raspberrypi.yml
    
install_ros:
  stage: deploy
  when: manual
  tags:
    - ansible
  script:
    - ansible-playbook -i hosts install_ros.yml
